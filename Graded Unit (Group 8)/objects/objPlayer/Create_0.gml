//creates speed values and gravity value when the object is created
hsp = 0;
vsp = 0;
grv = 0.3;
walksp = 4;

//tells object which layer to find the tiles for collision
tilemap = layer_tilemap_get_id("Tiles");